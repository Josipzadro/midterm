# Mutable list
mutable_list = [1, 2, 3, 4]

# Modifying the list in-place(we can do this without errors)
mutable_list[2] = 99


# Immutable string
immutable_string = "Hello"

# Attempting to modify the string with the line below will give an error
# immutable_string[1] = 'a'
